---
const navItems = [
  { name: 'Foods', href: '/foods' },
  { name: 'Amusements', href: '/amusements' },
  { name: 'About Us', href: '/about' },
  { name: 'Upcoming Events', href: '/upcoming' },
];
const leftItems = navItems.slice(0, 3);
const rightItems = navItems.slice(3);
---

<div class="header">
  <nav class="header-item nav-items-left" aria-label="Primary navigation">
    {
      leftItems.map((item) => (
        <a href={item.href} data-name={item.name}>
          {item.name}
        </a>
      ))
    }
  </nav>

  <div class="header-item logo">
    <a href="/"
      ><img src="fafficon.svg" alt="Fahie Amusements & Fair Foods home" />
    </a>
  </div>
  <nav class="header-item nav-items-right" aria-label="Secondary navigation">
    {
      rightItems.map((item) => (
        <a href={item.href} data-name={item.name}>
          {item.name}
        </a>
      ))
    }

    <a class="contact-button" href="/contact">Contact</a>
  </nav>

  <style>
    .header {
      position: absolute;
      font-family: var(--comfortaa);
      font-size: 1.5rem;
      display: flex;
      width: 100%;
      align-items: center;
      justify-content: center;
      z-index: 999;
    }
    .header-item {
      display: flex;
      align-items: center;
    }
    .nav-items-left,
    .nav-items-right {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .logo {
      position: absolute;
      top: 39vw;
    }
    .logo img {
      height: 25vw;
      width: auto;
    }
    @media (min-width: 600px) {

      .logo {
        position: absolute;
        top: 3vw;
      }
      .logo img {
        height: 10vw;
        width: auto;
      }
    }
    

    a {
      text-decoration: none;
      color: #fff;
      padding: 0.5em;
      margin: 1em;
    }

    a[data-name='Foods'] {
      --glow-color: red;
      text-shadow: 0 0 5px var(--glow-color),
          0 0 10px var(--glow-color),
          0 0 20px var(--glow-color);
    }
    a[data-name='Foods']:hover {
      animation: pulse 5s infinite;
    }
    a[data-name='Amusements'] {
      --glow-color: orange;
      text-shadow: 0 0 5px var(--glow-color),
          0 0 10px var(--glow-color),
          0 0 20px var(--glow-color);
    }
    a[data-name='Amusements']:hover {
      animation: pulse 5s infinite;
    }

    a[data-name='Upcoming Events'] {
      --glow-color: rgb(0, 255, 0);
      text-shadow: 0 0 5px var(--glow-color),
          0 0 10px var(--glow-color),
          0 0 20px var(--glow-color);
    }
    a[data-name='Upcoming Events']:hover {
      animation: pulse 5s infinite;
    }

    a[data-name='About Us'] {
      --glow-color: rgb(0, 0, 255);
      text-shadow: 0 0 5px var(--glow-color),
          0 0 10px var(--glow-color),
          0 0 20px var(--glow-color);
    }
    a[data-name='About Us']:hover {
      animation: pulse 5s infinite;
    }
    @keyframes pulse {
      0%, 74%, 79%,81%,100%{
        text-shadow:
          0 0 5px var(--glow-color, white),
          0 0 10px var(--glow-color, white),
          0 0 20px var(--glow-color, white),
          0 0 40px var(--glow-color, white),
          0 0 60px var(--glow-color, white);
      }
      75%,80%{
        text-shadow: none;
      }
    }

    .contact-button {
      padding: 1em;
      background-color: blue;
      border-radius: 5px;
    }
  </style>
</div>
